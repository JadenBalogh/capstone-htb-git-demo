import java.util.ArrayList;

public class Main {
  public static void main(String[] args) {
    int[] arr = { 1, 2, 3, 4 };
    System.out.println("Sum: " + getSum(arr));
    System.out.println("Mean: " + getSum(arr));
    System.out.println("Median: " + getSum(arr));
    System.out.println("Mode: " + getSum(arr));
  }

  // Assigned to Athena
  public static int getSum(int[] arr) {
    return 0;
  }

  // Assigned to Jared
  public static float getMean(int[] arr) {
    return 0;
  }

  // Assigned to Alvin
  public static float getMedian(int[] arr) {
    return 0;
  }

  // Assigned to Jaden
  public static int getMode(int[] arr) {
    // // Store the number of occurrences of each integer
    // ArrayList<> counts = new ArrayList<Integer>();
    // for (int i = 0; i < arr.length; i++) {
    //   int idx = arr[i];
    //   counts.set(idx, counts.get(idx) + 1);
    // }
    
    // // Find the max number of occurrences (return the first in case of tie)
    // int maxValue = arr[0];
    // int maxIdx = 0;
    // for (int i = 1; i < counts.size(); i++) {
    //   int count = counts.get(i);
    //   if (count > maxValue) {
    //     maxValue = count;
    //     maxIdx = i;
    //   }
    // }

    // Return the integer with max occurrences
    return 0;
  }
}